server:
  port: 8080

interview-atlas:
  ai:
    provider: "gemini"
    api-key: ${INTERVIEW_ATLAS_AI_API_KEY}
    base-url: ""
    model: "gemini-2.5-flash-lite"

management:
  endpoints:
    web:
      exposure:
        include: health
